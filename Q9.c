void main()
{
    float base = 10;
    float height = 5;
    float area;

    area = (base * height) / 2;

    printf("Base = %.2f\n", base);
    printf("Height = %.2f\n", height);
    printf("Area of Triangle = %.2f", area);
}